*.wav filter=lfs diff=lfs merge=lfs -text
*.sf2 filter=lfs diff=lfs merge=lfs -text

# GitHub blocks pushing new LFS objects through a public fork, so this small
# (181KB) bundled asset is committed as a regular blob instead - LFS stays on
# for any future large audio asset added with direct push access.
assets/audio/ir_room.wav filter= diff= merge= -text
